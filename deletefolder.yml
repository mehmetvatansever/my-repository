---
- name: Run to cmd
    hosts: all
  gather_facts: false

  tasks:
    - name: Save the result of 'whoami' in 'whoami_out'
        win_command: C:\Windows\System32\cmd.exe whoami
        register: whoami_out
